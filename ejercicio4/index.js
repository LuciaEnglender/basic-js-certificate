const myName = 'Lucía';
const lastname = 'Englender';
const estudiante = `${myName} ${lastname}`;
const estudianteMayus = estudiante.toUpperCase();
const estudianteMinus = estudiante.toLowerCase();
const longitud = estudiante.length;
const primeraLetraName = myName[0];
const ultimaLetraApellido = lastname[lastname.length - 1];
const sinEspacios = estudiante.trim();
const existName = estudiante.includes(myName);


